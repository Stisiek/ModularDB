import { PermnissionsPipe } from './permnissions.pipe';

describe('PermnissionsPipe', () => {
  it('create an instance', () => {
    const pipe = new PermnissionsPipe();
    expect(pipe).toBeTruthy();
  });
});
